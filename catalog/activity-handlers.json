{
  "version": "0.3.0",
  "handlers": {
    "classification.apply": {
      "description": "Deterministic orchestrator step: fold the classifier's primary decision into the structured input — type and cotSteps written in, requestedOutputs replaced (omitted when empty) — and return the merged record that replaces `input` for every later task. The file map rides through unchanged; the confirmation gate downstream may overwrite type and requestedOutputs with the submitter's choice.",
      "deterministic": true,
      "inputs": {
        "input": { "kind": "artifact", "schema": "processorOutput" },
        "classification": { "kind": "artifact", "schema": "taskClassification" },
        "maxFiles": { "kind": "positiveInteger" }
      },
      "outputSchema": "processorOutput",
      "bounds": {
        "outputField": "files",
        "maxItemsFromInput": "maxFiles"
      }
    },
    "attachments.useful": {
      "description": "Project the processor's annotated file map to the entries whose relation label is not NA, preserving order. This is the only file list later model calls receive.",
      "deterministic": true,
      "inputs": {
        "input": { "kind": "artifact", "schema": "processorOutput" },
        "maxFiles": { "kind": "positiveInteger" }
      },
      "outputSchema": "usefulFiles",
      "bounds": {
        "outputField": "files",
        "maxItemsFromInput": "maxFiles"
      }
    },
    "attachments.ignored": {
      "description": "Project the processor's annotated file map to the NA-labeled entries, preserving order. Kept as a separate audit artifact; never bound into later model calls.",
      "deterministic": true,
      "inputs": {
        "input": { "kind": "artifact", "schema": "processorOutput" },
        "maxFiles": { "kind": "positiveInteger" }
      },
      "outputSchema": "ignoredFiles",
      "bounds": {
        "outputField": "files",
        "maxItemsFromInput": "maxFiles"
      }
    },
    "attachments.code": {
      "description": "Project the useful files to the code-labeled entries (code / implementation), preserving order, plus their count. The count is what the workflow's condition tests to decide whether the code-annotation pass runs; the projection is the exact file list the annotator receives.",
      "deterministic": true,
      "inputs": {
        "files": { "kind": "artifact", "schema": "usefulFiles" },
        "maxFiles": { "kind": "positiveInteger" }
      },
      "outputSchema": "codeFiles",
      "bounds": {
        "outputField": "files",
        "maxItemsFromInput": "maxFiles"
      }
    },
    "attachments.annotate": {
      "description": "Deterministic orchestrator step: fold the code annotator's one-line per-file summaries into the shared useful-file map by exact path match — every summarized entry gains its codeSummary, every other entry rides through unchanged, and a summary naming an unknown path fails the run instead of being dropped silently. The merged map replaces usefulFiles, so every later task reads the annotated version.",
      "deterministic": true,
      "inputs": {
        "files": { "kind": "artifact", "schema": "usefulFiles" },
        "annotations": { "kind": "artifact", "schema": "codeAnnotations" },
        "maxFiles": { "kind": "positiveInteger" }
      },
      "outputSchema": "usefulFiles",
      "bounds": {
        "outputField": "files",
        "maxItemsFromInput": "maxFiles"
      }
    },
    "taxonomy.match": {
      "description": "Deterministic orchestrator step, no model call: the hybrid matching lane. Pillars decide — one taxonomy_resolve round-trip per pool member (exact name/alias match against the LIVE shared taxonomy). The semantic lane proposes — still-unmatched terms are scored against the registry-served node-embedding index (cosine plus a small active-region boost warmed by the classifier's facets and the exact-match landings, thresholds exactly as served in the embedder manifest): an unambiguous score above tauMatch auto-matches with matchedOn=embedding, everything else carries its scored top candidates for the placer to referee. Fail-safe: without a served index, or when the local embedder fails the served conformance vectors, the lane is off and matching behaves exactly as before it existed. Every answer carries the live taxonomy revision and the lane status.",
      "deterministic": true,
      "inputs": {
        "pool": { "kind": "artifact", "schema": "pool" },
        "classification": { "kind": "artifact", "schema": "taskClassification" },
        "maxMembers": { "kind": "positiveInteger" }
      },
      "outputSchema": "poolMatches",
      "bounds": {
        "outputField": "members",
        "maxItemsFromInput": "maxMembers"
      }
    },
    "taxonomy.suggest": {
      "description": "Deterministic orchestrator step: submit a copy of the run's decision for EVERY pool member (matched position, new placement, or already-present resolution) to the shared taxonomy service's append-only suggestion queue, and return the receipt. Suggestions are recorded, never applied — processing them is a separate server-side concern.",
      "deterministic": true,
      "inputs": {
        "matches": { "kind": "artifact", "schema": "poolMatches" },
        "placements": { "kind": "artifact", "schema": "placements" },
        "maxMembers": { "kind": "positiveInteger" }
      },
      "outputSchema": "suggestionReceipt",
      "bounds": {
        "outputField": "entries",
        "maxItemsFromInput": "maxMembers"
      }
    },
    "experts.bridge": {
      "description": "TEMPORARY seating bridge until the pool-based panel design lands: deterministically fold the matched positions and placement decisions into the legacy experts tree (department <- taxonomy field, umbrella <- subfield, leaf <- topic), summing each member's distinct-people count into its landing node and injecting the catch-all leaf for umbrella-level landings. Every node also carries the pool builder's input-topic relevance (the max over members landing at or below it), and EVERY level of the tree is sorted by that relevance, descending (ties by count, then name). The pool's literature grounding rides through onto the tree, so the dashboard's papers/authors/interests record stays. Keeps panel.select and everything downstream running unchanged.",
      "deterministic": true,
      "inputs": {
        "pool": { "kind": "artifact", "schema": "pool" },
        "matches": { "kind": "artifact", "schema": "poolMatches" },
        "placements": { "kind": "artifact", "schema": "placements" },
        "maxDepartments": { "kind": "positiveInteger" }
      },
      "outputSchema": "experts",
      "bounds": {
        "outputField": "departments",
        "maxItemsFromInput": "maxDepartments"
      }
    },
    "panel.select": {
      "description": "Deterministic seating over the PRUNED expertise tree: every level-2/3/4 node carries its cxr seating value (count x relevance; departments carry the sum over their umbrellas), all nodes are mixed into ONE queue sorted by cxr descending, and seats are popped while capacity remains — a topic seats a member with that single focus; an umbrella FIRST looks ahead and is skipped without spending capacity when any of its own topics sit within the next `capacity` live queue entries (the more specific seats win), otherwise it seats a member with itself and ALL its subfields joined by 'and', removing its topics from the queue; a department looks ahead the same way over its umbrellas and otherwise seats a member through its highest-cxr umbrella still available, with the union of the umbrella term and its subfields as focuses, removing that umbrella and its topics. Exhausted parents leave the queue recursively: consuming a node whose parent has no other child left removes the parent too, level by level. Seats are exact — several members may share an umbrella with different focuses, the identical focus set never seats twice; ties break by count then tree order; selection stops at panelSize or queue exhaustion. Relevance is required on every node — pre-relevance trees are refused and their runs restarted.",
      "deterministic": true,
      "inputs": {
        "experts": { "kind": "artifact", "schema": "experts" },
        "panelSize": { "kind": "positiveInteger" }
      },
      "outputSchema": "panel",
      "bounds": {
        "outputField": "members",
        "maxItemsFromInput": "panelSize"
      }
    },
    "panel.weave": {
      "description": "Deterministic seat weave, run after panel selection — no model call: append ONE interdisciplinary member whose expertise is the space BETWEEN the seated fields, derived from the seated members' umbrella terms (unique, in seat order) and marked seat=interdisciplinary. The member is a full panel member — it develops, is reviewed, and redevelops like every other seat; only its commenting skill differs, dispatched on the marker. Skipped (panel returned unchanged) when the panel spans fewer than two distinct umbrellas or when adding the seat would exceed maxSeats.",
      "deterministic": true,
      "inputs": {
        "panel": { "kind": "artifact", "schema": "panel" },
        "maxSeats": { "kind": "positiveInteger" }
      },
      "outputSchema": "panel",
      "bounds": {
        "outputField": "members",
        "maxItemsFromInput": "maxSeats"
      }
    }
  }
}
